version: "3.9"

services:
  agent-swarm:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./chroma_data:/app/chroma_data
      - ./data:/app/data
    environment:
      - CHROMA_PERSIST_DIR=/app/chroma_data
    restart: unless-stopped
